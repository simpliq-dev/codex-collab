name: Publish GitHub release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Check out tagged source
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build release kit
        run: npm run test-kit

      - name: Create portable archive
        id: kit
        shell: bash
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          KIT="markdown-collab-${VERSION}-kit"
          ARCHIVE="markdown-collab-${VERSION}.tar.gz"
          SKILL_ARCHIVE="markdown-collab-skill-${VERSION}.tar.gz"
          SKILL_FILE="SKILL.md"
          (cd release && tar -czf "${ARCHIVE}" "${KIT}")
          tar -czf "release/${SKILL_ARCHIVE}" -C "release/${KIT}/skills" markdown-collab
          cp "skills/markdown-collab/SKILL.md" "release/${SKILL_FILE}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "kit=${KIT}" >> "$GITHUB_OUTPUT"
          echo "archive=${ARCHIVE}" >> "$GITHUB_OUTPUT"
          echo "skill_archive=${SKILL_ARCHIVE}" >> "$GITHUB_OUTPUT"
          echo "skill_file=${SKILL_FILE}" >> "$GITHUB_OUTPUT"

      - name: Publish release assets
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
          VERSION: ${{ steps.kit.outputs.version }}
          KIT: ${{ steps.kit.outputs.kit }}
          ARCHIVE: ${{ steps.kit.outputs.archive }}
          SKILL_ARCHIVE: ${{ steps.kit.outputs.skill_archive }}
          SKILL_FILE: ${{ steps.kit.outputs.skill_file }}
        shell: bash
        run: |
          PRERELEASE=()
          if [[ "$TAG" == *-* ]]; then
            PRERELEASE+=(--prerelease)
          fi

          TITLE="Markdown Collab ${VERSION}"
          if [[ "$TAG" != "v${VERSION}" ]]; then
            echo "Tag ${TAG} does not match package version ${VERSION}." >&2
            exit 1
          fi
          NOTES="Install the VSIX directly, or download the complete release kit. SKILL.md is attached as a direct download for manual agent setup, and the same skill is bundled inside the VSIX for guided installation."
          VSIX="release/${KIT}/markdown-collab-${VERSION}.vsix"
          ARCHIVE="release/${ARCHIVE}"
          SKILL="release/${SKILL_ARCHIVE}"
          SKILL_MD="release/${SKILL_FILE}"

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$VSIX" "$ARCHIVE" "$SKILL" "$SKILL_MD" --clobber
          else
            gh release create "$TAG" "$VSIX" "$ARCHIVE" "$SKILL" "$SKILL_MD" \
              --verify-tag \
              --title "$TITLE" \
              --notes "$NOTES" \
              "${PRERELEASE[@]}"
          fi
